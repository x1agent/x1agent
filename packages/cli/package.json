{
  "name": "@x1agent/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "x1": "./src/index.ts"
  },
  "scripts": {
    "setup": "bun run src/index.ts setup",
    "configure": "bun run src/index.ts configure",
    "configure:check": "bun run src/index.ts configure:check",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.8.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
