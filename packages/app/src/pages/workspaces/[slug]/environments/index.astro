---
import Layout from "../../../../layouts/Layout.astro";
import { EnvironmentsRoot } from "../../../../features/environments/EnvironmentsRoot";

const { slug } = Astro.params;
---

<Layout title={`Environments · ${slug} · x1agent`}>
  <EnvironmentsRoot client:only="react" workspaceSlug={slug ?? ""} />
</Layout>
