---
import Layout from "../../../../layouts/Layout.astro";
import { AgentEditRoot } from "../../../../features/agents/AgentEditRoot";

const { slug, agentSlug } = Astro.params;
---

<Layout title={`${agentSlug} · x1agent`}>
  <AgentEditRoot
    client:only="react"
    workspaceSlug={slug ?? ""}
    agentSlug={agentSlug ?? ""}
  />
</Layout>
